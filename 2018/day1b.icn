procedure main()

    freq := 0
    inputs := []
    seen := set([0])

    while put(inputs,trim(read()))

    repeat {
        every freq +:= !inputs do {
            if member(seen,freq) then break break
            insert(seen,freq)
        }
    }

    write(freq)
end